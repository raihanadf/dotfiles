# Raihan's config for i3

# ========= Initial ==========

set $term kitty

# mouse
focus_follows_mouse no
exec_always --no-startup-id xinput set-prop 14 325 1
exec_always --no-startup-id xinput set-prop 14 345 1

# necessary apps
exec_always --no-startup-id nitrogen --restore
# exec_always --no-startup-id picom
exec --no-startup-id redshift -P -O 5500K
exec --no-startup-id dunst
exec --no-startup-id ~/.dotfiles/scripts/udiskie.sh
exec --no-startup-id ~/.dotfiles/scripts/batteryCheck.sh
# exec --no-startup-id nm-applet
# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork -i $wpfolder/pixel-space.jpg
# set $refresh_i3status killall -SIGUSR1 i3status

# ========== Border / Gaps ==========

# Borders
default_border pixel 3
set $gaps 15
#gaps inner $gaps
gaps inner 10
gaps outer 15

# Smart Thingy
# smart_gaps on
smart_borders no_gaps

# ========== Colors ==========

# colors
# class                  border  backgr. text    indicator child_border
client.focused           #383838 #928373 #ebdbb2 #383838   #383838
client.urgent            #928373 #928373 #ebdbb2 #928373   #928373
client.focused_inactive  #383838 #272727 #ebdbb2 #383838   #383838
client.unfocused         #272727 #272727 #928373 #292d2e   #222222
client.background        #272727 


# ========== Navigating ==========

set $mod Mod1

set $left h
set $down j
set $up k
set $right l

# ========== Fonts ==========

#font pango:monospace 8
font pango: CaskaydiaCove NF 10

# ========== Binding ==========

# Alt-tab like behaviour
bindsym Mod1+Tab workspace back_and_forth

# Audio
bindsym XF86AudioRaiseVolume exec --no-startup-id ~/.dotfiles/scripts/volume.sh up
bindsym XF86AudioLowerVolume exec --no-startup-id ~/.dotfiles/scripts/volume.sh down
bindsym XF86AudioMute exec --no-startup-id ~/.dotfiles/scripts/volume.sh mute
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE @ toggle
# Audio but w/ super key
bindsym $mod+Control+apostrophe exec --no-startup-id ~/.dotfiles/scripts/volume.sh up
bindsym $mod+Control+semicolon exec --no-startup-id ~/.dotfiles/scripts/volume.sh down

# Brightness
bindsym XF86MonBrightnessUp exec --no-startup-id ~/.dotfiles/scripts/backlight.sh up
bindsym XF86MonBrightnessDown exec --no-startup-id ~/.dotfiles/scripts/backlight.sh down
# Brightness but w/ super key
bindsym $mod+Control+slash exec --no-startup-id ~/.dotfiles/scripts/backlight.sh up
bindsym $mod+Control+period exec --no-startup-id ~/.dotfiles/scripts/backlight.sh down

# Network
bindsym $mod+n exec --no-startup-id networkmanager_dmenu

# Screenshot
bindsym Print exec --no-startup-id flameshot full -p ~/Pictures/Screenshots
bindsym $mod+u --release exec --no-startup-id flameshot gui -p ~/Pictures/Screenshots

# Lock Screen
bindsym $mod+shift+x exec --no-startup-id i3lock -i $wpfolder/pixel-space.jpg

# Wallpaper
set $wpfolder ~/Pictures/Wallpapers
bindsym $mod+Shift+semicolon exec --no-startup-id nitrogen $wpfolder || notify-send "Theres no 'Wallpapers' folder in Pictures"

# Kill some fuckers
bindsym $mod+Ctrl+x --release exec --no-startup-id notify-send "IT'S XKILL BOIS!!!" "Select a dumb app to kill" -i dialog-error && xkill

# Pomodoro 
bindsym $mod+Control+p exec --no-startup-id ~/.dotfiles/scripts/pomo.sh

# Floating Modifier to Move Window
floating_modifier $mod

# Terminal
bindsym $mod+Return exec --no-startup-id $term

# Kill Window
bindsym $mod+Shift+q kill

# Program Launcher / Menu
#bindsym $mod+d exec --no-startup-id dmenu_run
bindsym $mod+d exec "rofi -modi drun,run -show drun"
# bindcode $mod+40 exec --no-startup-id i3-dmenu-desktop

# Change Focus
bindsym $mod+$left focus left
bindsym $mod+$down focus down
bindsym $mod+$up focus up
bindsym $mod+$right focus right

# Change Focus but with arrow keys
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# Move Focused Window
bindsym $mod+Shift+$left move left
bindsym $mod+Shift+$down move down
bindsym $mod+Shift+$up move up
bindsym $mod+Shift+$right move right

# Move Focused Window but with arrow keys
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# Split Horizontal
bindsym $mod+b split h

# Split Vertical
bindsym $mod+v split v

# Enter Fullscreen
bindsym $mod+f fullscreen toggle

# Change Container Layout
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# Toggle Floating
bindsym $mod+Shift+space floating toggle

# Toggle Focus between Tiling or Floating
bindsym $mod+space focus mode_toggle

# Focus to parent container
bindsym $mod+a focus parent

# Focus to child container
#bindsym $mod+d focus child

# ========== Workspace ==========
set $ws1 "1:term"
set $ws2 "2:dev"
set $ws3 "3:web"
set $ws4 "4:media"
set $ws5 "5:zoom"
set $ws6 "6:tor"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10:float"

# Switch to Workspaces
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# Move Focused Container to Workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# ========== Config ==========
# Reload the configuration file
bindsym $mod+Shift+c reload
# Restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# Exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# ========== Resizing ==========
# Resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym $left resize shrink width 10 px or 10 ppt
        bindsym $down resize grow height 10 px or 10 ppt
        bindsym $up resize shrink height 10 px or 10 ppt
        bindsym $right resize grow width 10 px or 10 ppt

        bindsym Shift + $left resize shrink width 50 px or 50 ppt
        bindsym Shift + $down resize grow height 50 px or 50 ppt
        bindsym Shift + $up resize shrink height 50 px or 50 ppt
        bindsym Shift + $right resize grow width 50 px or 50 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"
# Control gaps
#bindsym $mod+g        gaps inner current plus 5
#bindsym $mod+Shift+g  gaps inner current minus 5
#bindsym $mod+Control+g  gaps inner current set $gaps

# ========== i3's Bar ==========

# bar {
# 	status_command i3status
# 	font pango:CaskaydiaCove NF 11
# 	separator_symbol |
# 
#   colors {
#     background #010D1A
#     statusline #FFFFFF
#     separator  #FFFA55
# 
#     focused_workspace  #BEB093 #323364 #FFFFFF
#     active_workspace   #333333 #222222 #FFFFFF
#     inactive_workspace #333333 #222222 #888888
#     urgent_workspace   #2F343A #900000 #FFFFFF
#     binding_mode       #2F343A #900000 #FFFFFF
#   }
# 
# }

exec_always --no-startup-id $HOME/.config/polybar/launch.sh

# ========== App's Workspace ==========
for_window [urgent=latest] focus
for_window[window_role="Dialog*"] focus

# ===== workspace 1 ( terminal )=====
#
# well idk
#
# ===== workspace 2 ( dev )=====

# // android studio
for_window [class="jetbrains-*" title="^win\d+$"] focus
for_window [class="jetbrains-*"] move to workspace $ws2

# // code
for_window [class="(?i)code"] move to workspace $ws2

# // blender
for_window [class="(?i)blender"] move to workspace $ws2

# // netbeans
for_window [class="(?i)netbeans"] move to workspace $ws2

# // browser pop-ups
for_window [window_role="pop-up"] floating enable

# ===== workspace 3 ( browser )=====

# // firefox
for_window [class="firefox"] move to workspace $ws3

# // chrome
for_window [class="(?i)google-*"] move to workspace $ws3


# ===== workspace 4 =====

# // spotify
for_window [class="(?i)spotify"] move to workspace $ws4

# // discord
for_window [class="discord"] move to workspace $ws4

# ===== workspace 5 =====

# // zoom
for_window [class="zoom"] floating enable
for_window [class="zoom"] move to workspace $ws5


# ===== workspace 6 =====
for_window [class="(?i)qbittorrent"] move to workspace $ws6

# ===== workspace 7 =====
#
# ===== workspace 8 =====
#
# ===== workspace 9 =====

# // steam
for_window [class="(?i)steam"] move to workspace $ws9
for_window [workspace="9"] floating enable

# ===== workspace 10 =====

# set every window to floating mode
for_window [workspace="10"] floating enable

