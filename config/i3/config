# Raihan's config for i3

# ========= Initial ==========

focus_follows_mouse no
exec_always --no-startup-id nitrogen --restore
exec_always --no-startup-id picom
exec_always --no-startup-id udiskie &
exec --no-startup-id dunst
#exec --no-startup-id nm-applet
# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork
set $refresh_i3status killall -SIGUSR1 i3status

# ========== Border / Gaps ==========
 
default_border pixel 2
gaps inner 5

# ========== Colors ==========

set $cl_high #006660
set $cl_indi #d9d8d8
set $cl_back #011211
set $cl_fore #ADD8E6
set $cl_urge #ee2e24

#sections               border   backgr   text     indi     childborder
client.focused          $cl_high $cl_back $cl_high $cl_high $cl_high
client.focused_inactive $cl_back $cl_back $cl_fore $cl_back $cl_back
client.unfocused        $cl_back $cl_back $cl_fore $cl_back $cl_back
client.urgent           $cl_urge $cl_urge $cl_fore $cl_urge $cl_urge


# ========== Navigating ==========

set $mod Mod1

set $left h
set $down j
set $up k
set $right l

# ========== Fonts ==========
#font pango:monospace 8
font pango: CaskaydiaCove NF 10


# ========== Binding ==========

# Audio
bindsym XF86AudioRaiseVolume exec --no-startup-id ~/.dotfiles/scripts/volume.sh up
bindsym XF86AudioLowerVolume exec --no-startup-id ~/.dotfiles/scripts/volume.sh down
bindsym XF86AudioMute exec --no-startup-id ~/.dotfiles/scripts/volume.sh mute
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE @ toggle
# Brightness
bindsym XF86MonBrightnessUp exec --no-startup-id ~/.dotfiles/scripts/backlight.sh up
bindsym XF86MonBrightnessDown exec --no-startup-id ~/.dotfiles/scripts/backlight.sh down
# Screenshot
bindsym Print exec --no-startup-id flameshot gui
# Floating Modifier to Move Window
floating_modifier $mod
# Terminal
bindsym $mod+Return exec kitty
# Kill Window
bindsym $mod+Shift+q kill
# Program Launcher / Menu
#bindsym $mod+d exec --no-startup-id dmenu_run
bindsym $mod+d exec "rofi -modi drun,run -show drun"
# bindcode $mod+40 exec --no-startup-id i3-dmenu-desktop
# Change Focus
bindsym $mod+$left focus left
bindsym $mod+$down focus down
bindsym $mod+$up focus up
bindsym $mod+$right focus right
# Change Focus but with arrow keys
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right
# Move Focused Window
bindsym $mod+Shift+$left move left
bindsym $mod+Shift+$down move down
bindsym $mod+Shift+$up move up
bindsym $mod+Shift+$right move right
# Move Focused Window but with arrow keys
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right
# Split Horizontal
bindsym $mod+b split h
# Split Vertical
bindsym $mod+v split v
# Enter Fullscreen
bindsym $mod+f fullscreen toggle
# Change Container Layout
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split
# Toggle Floating
bindsym $mod+Shift+space floating toggle
# Toggle Focus between Tiling or Floating
bindsym $mod+space focus mode_toggle
# Focus to parent container
bindsym $mod+a focus parent
# Focus to child container
#bindsym $mod+d focus child

# ========== Workspace ==========
set $ws1 "1:I"
set $ws2 "2:II"
set $ws3 "3:III"
set $ws4 "4:IV"
set $ws5 "5:V"
set $ws6 "6:VI"
set $ws7 "7:VII"
set $ws8 "8:VIII"
set $ws9 "9:IX"
set $ws10 "10:X"

# Switch to Workspaces
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# Move Focused Container to Workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# ========== Config ==========
# Reload the configuration file
bindsym $mod+Shift+c reload
# Restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# Exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# ========== Resizing ==========
# Resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym $left resize shrink width 10 px or 10 ppt
        bindsym $down resize grow height 10 px or 10 ppt
        bindsym $up resize shrink height 10 px or 10 ppt
        bindsym $right resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# ========== i3's Bar ==========

#bar {
#        status_command i3status
#}

exec_always --no-startup-id $HOME/.config/polybar/launch.sh

# ========== App's Workspace ==========

# ===== workspace 1 ( terminal )=====
#
# well idk
#
# ===== workspace 2 ( browser )=====

# // firefox
for_window [class="firefox"] move to workspace $ws2

# // browser pop-ups
for_window [window_role="pop-up"] floating enable

# ===== workspace 3 ( development )=====

# // android studio
for_window [class="jetbrains-studio" title="^win\d+$"] focus
for_window [class="jetbrains-studio"] move to workspace $ws3

# // code
for_window [class="(?i)code"] move to workspace $ws3

# ===== workspace 4 =====

# discord
for_window [class="discord"] move to workspace $ws4

# spotify
for_window [class="(?i)spotify"] move to workspace $ws4

# ===== workspace 5 =====
# zoom

for_window [class="zoom"] floating enable
for_window [class="zoom"] move to workspace $ws5

# ===== workspace 6 and so on =====
#
