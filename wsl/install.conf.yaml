- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    # files 
    ~/.config/starship.toml: ../config/starship.toml
    ~/.vimrc: ../dots/vimrc
    ~/.zshrc: ../dots/zshrc
    ~/.p10k.zsh: ../dots/p10k.zsh
    ~/.gitconfig: ../dots/gitconfig
    ~/.zsh_aliases: ../dots/zsh_aliases
    ~/.zsh_exports: ../dots/zsh_exports
    ~/.tmux.conf: ../dots/tmux.conf

    # nvim
    ~/.config/nvim:
      create: true
      path: ../config/nvim
      force: true

    # ~/.vim:
    
    #  create: true
    #  path: config/nvim
    #  force: true
  
#in case if you forget, it needed 4 spaces or 2 tabs


